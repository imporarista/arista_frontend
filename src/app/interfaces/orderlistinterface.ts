export interface Orderlistinterface {
    // fecha de la orden
    date: string;
    // id de la orden para obtener los detalles de productos
    id: number;
    // valor de los productos comprados
    amount: number;
}
